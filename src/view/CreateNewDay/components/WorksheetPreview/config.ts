import { EventType } from '@models/block'

export const eventTypesMap: Record<EventType, string> = {
    for_time: 'For Time',
    amrap: 'AMRAP',
    emom: 'EMOM',
    max_weight: 'Carga máxima',
}
